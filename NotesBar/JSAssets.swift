import Foundation

struct JSAssets {
    static let mermaidScript = "https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"

    // We can also bundle the strings here for true offline support if needed.
    // For now, using CDNs for clarity and smaller binary size.
}
